{
  "name": "virasat",
  "version": "0.1.0",
  "private": true,
  "description": "Kannada, Hindi, and English voice interview that maps estate claims deterministically.",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot web/serve.ts",
    "web": "bun run --hot web/serve.ts",
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "convex": "bunx convex dev",
    "typecheck": "tsc --noEmit",
    "typecheck:worker": "tsc --project worker/tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "convex": "^1.17.0",
    "fflate": "^0.8.3",
    "openai": "^6.49.0",
    "sarvamai": "^1.1.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260726.1",
    "@types/bun": "latest",
    "typescript": "^5.6.0",
    "wrangler": "^4.114.0"
  }
}
